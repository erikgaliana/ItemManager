import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-item-manager',
  webDir: '../../dist/apps/ion-item-manager',
  bundledWebRuntime: false,
};

export default config;
